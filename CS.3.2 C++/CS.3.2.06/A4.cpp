#include <iostream>
using namespace std;

int main () {
    int *p = 0;
    int * q = nullptr;
    int * r = NULL;

    printf("p = %d\n", p);
    printf("q = %d\n", q);
    printf("r = %d", r);
}